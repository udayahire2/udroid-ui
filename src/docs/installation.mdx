import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";


# Installation

<div className="lead text-lg text-muted-foreground mb-8">
  Get up and running with UDROID UI in less than 5 minutes.
  We support all major React frameworks including **Next.js**, **Vite**, and **Remix**.
</div>

<Tabs defaultValue="next-js" className="mt-6">
  <TabsList className="grid w-full grid-cols-3 max-w-[400px]">
    <TabsTrigger value="next-js">Next.js</TabsTrigger>
    <TabsTrigger value="vite">Vite</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  
  <TabsContent value="next-js" className="mt-6">
    <div className="space-y-6">
      <div>
        <h3 className="text-lg font-semibold mb-2">1. Create Project</h3>
        <p className="text-muted-foreground mb-3">Start by creating a new Next.js project using `create-next-app`.</p>
        ```bash
        npx create-next-app@latest my-app --typescript --tailwind --eslint
        ```
      </div>

      <div>
        <h3 className="text-lg font-semibold mb-2">2. Initialize UDROID</h3>
        <p className="text-muted-foreground mb-3">Run the init command to configure your project.</p>
        ```bash
        npx udroid-ui init
        ```
      </div>

      <div>
        <h3 className="text-lg font-semibold mb-2">3. Start Building</h3>
        <p className="text-muted-foreground mb-3">You can now start adding components to your project.</p>
        ```bash
        npx udroid-ui add button
        ```
      </div>
    </div>
  </TabsContent>
  
  <TabsContent value="vite" className="mt-6">
    <div className="space-y-6">
      <div>
        <h3 className="text-lg font-semibold mb-2">1. Create Project</h3>
        <p className="text-muted-foreground mb-3">Initialize a new Vite + React project.</p>
        ```bash
        npm create vite@latest my-app -- --template react-ts
        ```
      </div>

      <div>
        <h3 className="text-lg font-semibold mb-2">2. Setup Tailwind</h3>
        <p className="text-muted-foreground mb-3">Install Tailwind CSS and its peer dependencies.</p>
        ```bash
        npm install -D tailwindcss postcss autoprefixer
        npx tailwindcss init -p
        ```
      </div>

      <div>
        <h3 className="text-lg font-semibold mb-2">3. Initialize UDROID</h3>
        <p className="text-muted-foreground mb-3">Run the CLI wizard to set up your design system.</p>
        ```bash
        npx udroid-ui init
        ```
      </div>
    </div>
  </TabsContent>
  
  <TabsContent value="manual" className="mt-6">
     <div className="rounded-lg border border-yellow-500/20 bg-yellow-500/10 p-4 text-yellow-600 dark:text-yellow-400">
       <strong>Note:</strong> Manual installation is for advanced users who want full control over their dependencies.
     </div>
     
     
     <div className="space-y-6 mt-6">
       <div>
         <h3 className="text-lg font-semibold mb-2">1. Install Dependencies</h3>
         <p className="text-muted-foreground mb-3">Install the core utility libraries.</p>
         ```bash
         npm install clsx tailwind-merge cva framer-motion lucide-react
         ```
       </div>
       
       <div>
         <h3 className="text-lg font-semibold mb-2">2. Add Utils</h3>
         <p className="text-muted-foreground mb-3">Create a `lib/utils.ts` file for class merging.</p>
       </div>
       
       <div>
         <h3 className="text-lg font-semibold mb-2">3. Configure Tailwind</h3>
         <p className="text-muted-foreground mb-3">Add the `animate` plugin to your `tailwind.config.js`.</p>
       </div>
     </div>
  </TabsContent>
</Tabs>
