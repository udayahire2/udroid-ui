# Switch

A control that allows the user to toggle between checked and not checked.

## Variants & States

<ComponentPreview name="switch-variants" />

## Premium Card (Context)

<ComponentPreview name="switch-card" />

## Installation

```bash
npm install @radix-ui/react-switch
```

## API Reference

### Switch

The root component. Accepts all props from `@radix-ui/react-switch`.

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `variant` | `'default' \| 'premium'` | `'default'` | The visual style. |
